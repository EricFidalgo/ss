<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Homework2.Maui.Models"
             x:Class="Homework2.Maui.Views.AppointmentListPage"
             Title="Appointments"
             BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add Appointment" 
                     Clicked="OnAddAppointmentClicked"
                     Order="Primary"
                     Priority="0">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="+" 
                                 FontFamily="OpenSansRegular"
                                 Color="{StaticResource Primary}"/>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*" Padding="15">
        <Frame Grid.Row="0" 
               BackgroundColor="{StaticResource Primary}"
               CornerRadius="15"
               Padding="20"
               Margin="0,0,0,15"
               HasShadow="True">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="15">
                <Frame Grid.Column="0"
                       BackgroundColor="White"
                       CornerRadius="30"
                       Padding="15"
                       HasShadow="False"
                       WidthRequest="60"
                       HeightRequest="60">
                    <Label Text="📅" 
                           FontSize="30"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                </Frame>
                <VerticalStackLayout Grid.Column="1" 
                                     VerticalOptions="Center"
                                     Spacing="5">
                    <Label Text="Appointment Management" 
                           FontSize="22" 
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label x:Name="AppointmentCountLabel"
                           Text="0 appointments scheduled" 
                           FontSize="15"
                           TextColor="White"
                           Opacity="0.9"/>
                </VerticalStackLayout>
            </Grid>
        </Frame>

        <CollectionView Grid.Row="1"
                        x:Name="appointmentsCollectionView"
                        SelectionMode="None"
                        EmptyView="No appointments scheduled. Tap 'Add Appointment' to create one.">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Appointment">
                    <Frame Padding="0" 
                           Margin="0,0,0,12" 
                           BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                           BorderColor="{StaticResource Gray200}"
                           CornerRadius="15"
                           HasShadow="True">
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" 
                              ColumnDefinitions="Auto,*"
                              Padding="15"
                              RowSpacing="12"
                              ColumnSpacing="12">

                            <Frame Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                                   BackgroundColor="{StaticResource Primary}"
                                   CornerRadius="12"
                                   Padding="10"
                                   HasShadow="False"
                                   WidthRequest="70"
                                   HeightRequest="70"
                                   VerticalOptions="Start">
                                <VerticalStackLayout Spacing="0" 
                                                     HorizontalOptions="Center"
                                                     VerticalOptions="Center">
                                    <Label Text="{Binding hour, StringFormat='{0:MMM}'}" 
                                           FontSize="12"
                                           FontAttributes="Bold"
                                           TextColor="White"
                                           HorizontalOptions="Center"/>
                                    <Label Text="{Binding hour, StringFormat='{0:dd}'}" 
                                           FontSize="24"
                                           FontAttributes="Bold"
                                           TextColor="White"
                                           HorizontalOptions="Center"/>
                                </VerticalStackLayout>
                            </Frame>
                            
                            <Label x:Name="TimeLabel"
                                   Grid.Row="0" Grid.Column="1"
                                   Text="{Binding hour, StringFormat='{0:h:mm tt}'}" 
                                   IsVisible="{Binding IsNotEditing}"
                                   FontSize="20" 
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                                   VerticalOptions="End"/>
                            
                            <Button x:Name="TimeButton"
                                    Grid.Row="0" Grid.Column="1"
                                    Text="{Binding hour, StringFormat='{0:h:mm tt}'}"
                                    IsVisible="{Binding IsEditing}"
                                    Clicked="OnTimeButtonClicked"
                                    FontSize="14"
                                    Padding="8,5"
                                    VerticalOptions="End"
                                    HorizontalOptions="Start"
                                    BackgroundColor="{StaticResource Primary}"
                                    TextColor="White"/>
                            
                            <Label x:Name="DateLabel"
                                   Grid.Row="1" Grid.Column="1"
                                   Text="{Binding hour, StringFormat='{0:dddd, MMMM dd, yyyy}'}" 
                                   IsVisible="{Binding IsNotEditing}"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray600}"
                                   VerticalOptions="Start"/>
                            
                            <DatePicker x:Name="DatePicker"
                                        Grid.Row="1" Grid.Column="1"
                                        Date="{Binding hour}"
                                        IsVisible="{Binding IsEditing}"
                                        FontSize="13"
                                        VerticalOptions="Start"/>
                            
                            <BoxView Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                     HeightRequest="1"
                                     Color="{StaticResource Gray200}"
                                     Margin="0,5,0,5"/>
                            
                            <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                  RowDefinitions="Auto,Auto"
                                  ColumnDefinitions="Auto,*"
                                  RowSpacing="10"
                                  ColumnSpacing="10">
                                
                                <Frame Grid.Row="0" Grid.Column="0"
                                       BackgroundColor="{StaticResource Secondary}"
                                       CornerRadius="20"
                                       Padding="8"
                                       HasShadow="False"
                                       WidthRequest="36"
                                       HeightRequest="36">
                                    <Label Text="👤" FontSize="16" 
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"/>
                                </Frame>
                                <VerticalStackLayout Grid.Row="0" Grid.Column="1" 
                                                     VerticalOptions="Center">
                                    <Label Text="Patient" 
                                           FontSize="11"
                                           TextColor="{StaticResource Gray500}"
                                           FontAttributes="Bold"/>
                                    <Label x:Name="PatientLabel"
                                           Text="{Binding patients.name}" 
                                           IsVisible="{Binding IsNotEditing}"
                                           FontSize="15"
                                           FontAttributes="Bold"
                                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                                    <Button x:Name="PatientButton"
                                            Text="{Binding patients.name, TargetNullValue='Select Patient'}"
                                            IsVisible="{Binding IsEditing}"
                                            Clicked="OnPatientButtonClicked"
                                            FontSize="13"
                                            Padding="8,4"
                                            HorizontalOptions="Start"
                                            BackgroundColor="{StaticResource Primary}"
                                            TextColor="White"/>
                                </VerticalStackLayout>
                                
                                <Frame Grid.Row="1" Grid.Column="0"
                                       BackgroundColor="{StaticResource Secondary}"
                                       CornerRadius="20"
                                       Padding="8"
                                       HasShadow="False"
                                       WidthRequest="36"
                                       HeightRequest="36">
                                    <Label Text="🩺" FontSize="16"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"/>
                                </Frame>
                                <VerticalStackLayout Grid.Row="1" Grid.Column="1"
                                                     VerticalOptions="Center">
                                    <Label Text="Physician" 
                                           FontSize="11"
                                           TextColor="{StaticResource Gray500}"
                                           FontAttributes="Bold"/>
                                    <Label x:Name="PhysicianLabel"
                                           Text="{Binding physicians.name, StringFormat='Dr. {0}'}" 
                                           IsVisible="{Binding IsNotEditing}"
                                           FontSize="15"
                                           FontAttributes="Bold"
                                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                                    <Button x:Name="PhysicianButton"
                                            Text="{Binding physicians.name, StringFormat='Dr. {0}', TargetNullValue='Select Physician'}"
                                            IsVisible="{Binding IsEditing}"
                                            Clicked="OnPhysicianButtonClicked"
                                            FontSize="13"
                                            Padding="8,4"
                                            HorizontalOptions="Start"
                                            BackgroundColor="{StaticResource Primary}"
                                            TextColor="White"/>
                                </VerticalStackLayout>
                            </Grid>

                            <Label x:Name="ErrorLabel"
                                   Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                                   IsVisible="False"
                                   Text=""
                                   TextColor="Red"
                                   FontSize="12"
                                   HorizontalOptions="Center"/>

                            <Grid Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                                  ColumnDefinitions="*,*,*"
                                  ColumnSpacing="8"
                                  Margin="0,10,0,0">
                                <Button x:Name="EditInlineButton"
                                        Grid.Column="0"
                                        Text="✏️ Edit Inline"
                                        Clicked="OnInlineEditClicked"
                                        CommandParameter="{Binding .}"
                                        FontSize="12"
                                        Padding="8,5"
                                        BackgroundColor="{StaticResource Primary}"
                                        TextColor="White"
                                        CornerRadius="8"/>
                                <Button Grid.Column="1"
                                        Text="📝 Edit Dialog"
                                        Clicked="OnDialogEditClicked"
                                        CommandParameter="{Binding .}"
                                        FontSize="12"
                                        Padding="8,5"
                                        BackgroundColor="{StaticResource Secondary}"
                                        TextColor="{StaticResource Primary}"
                                        CornerRadius="8"/>
                                <Button Grid.Column="2"
                                        Text="🗑️ Delete"
                                        Clicked="OnInlineDeleteClicked"
                                        CommandParameter="{Binding .}"
                                        FontSize="12"
                                        Padding="8,5"
                                        BackgroundColor="#DC3545"
                                        TextColor="White"
                                        CornerRadius="8"/>
                            </Grid>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>